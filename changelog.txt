---------------- JS Tutorial chagngelog -------------------------
add Variable page {
 add Variable topic
} 
add Getstart page {
    create Getstart page
} 
add Data data types page {
   add all data types
}
add operator page {
   add all operator 
}
add stetment page {
   add if anf else stetment
}
add Es6 page {
    add Inheritance topic
    add super() methood
    add Method Overriding
}
  let arrays = [];
  let forUserRes = prompt('how much your girl Friend');
  for (let i = 0; i < forUserRes; i++) {
    arrays[i] = prompt('enter your girl Friend name');
  }
  for (let i = 0; i < forUserRes; i++) {
    document.write(`<h1>this is your girl Friend : ${arrays[i]}</h1><br>`);
  }